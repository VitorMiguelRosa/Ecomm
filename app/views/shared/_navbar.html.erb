<nav class="fixed w-full top-0 bg-[var(--cor-logo)] z-10 py-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between relative h-16 px-4 lg:px-0">
      <!-- Left side -->
      <div class="flex items-center space-x-10">
        <%= link_to "Shop", root_path, class: "text-gray-300 hover:text-gray-50 py-2 rounded-md text-xl font-medium hover:text-2xl transition-all duration-200" %>
      </div>
      <!-- Center logo -->
      <div class="flex-shrink-0 flex items-center">
        <%= link_to root_path do %>
          <%= image_tag 'minhalogo.png', class: 'block lg:hidden h-20 w-auto' %>
        <% end %>
        <%= link_to root_path do %>
          <%= image_tag 'minhalogo.png', class: 'hidden lg:block h-20 w-auto' %>
        <% end %>
      </div>
      <!-- Right side -->
      <div class="flex items-center space-x-10">
        <%= link_to cart_path, class:"text-gray-300 hover:text-gray-50 py-2 rounded-md text-xl font-medium hover:text-2xl transition-all duration-200" do %>
          <%= icon "fa-solid", "shopping-cart" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
</nav>